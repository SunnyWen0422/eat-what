server:
  port: ${SERVER_PORT:8080}
  servlet:
    context-path: /api

spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://127.0.0.1:3306/food?useSSL=false&serverTimezone=Asia/Shanghai}
    username: ${DB_USER:food}
    password: ${DB_PASSWORD:change-me}
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: ${DB_POOL_MAX:10}
      minimum-idle: ${DB_POOL_MIN:2}
      connection-timeout: 30000
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
    default-property-inclusion: non_null

mybatis:
  type-aliases-package: com.eatwhat.entity
  mapper-locations: classpath:mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true

wechat:
  miniapp:
    appid: ${WECHAT_APPID:change-me}
    secret: ${WECHAT_SECRET:change-me}

logging:
  level:
    com.eatwhat: info
    org.springframework.web: info

