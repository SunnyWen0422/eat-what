/* pages/about/about.wxss */

.about-page {
  background-color: #f5f5f5;
  min-height: 100vh;
  padding-bottom: 40px;
}

/* 顶部标题区 */
.header {
  background: linear-gradient(135deg, #3cc51f 0%, #2e8b57 100%);
  padding: 40px 20px 30px;
  display: flex;
  flex-direction: column;
  align-items: center;
  color: white;
}

.app-name {
  font-size: 32px;
  font-weight: bold;
  margin-bottom: 8px;
}

.app-slogan {
  font-size: 18px;
  opacity: 0.9;
  margin-bottom: 12px;
}

.version {
  font-size: 14px;
  opacity: 0.7;
  background: rgba(255, 255, 255, 0.2);
  padding: 4px 12px;
  border-radius: 12px;
}

/*  section标题 */
.section-title {
  font-size: 20px;
  font-weight: bold;
  color: #333;
  padding: 20px 20px 12px;
}

/* 步骤卡片 */
.step-card {
  background-color: white;
  margin: 0 20px 16px;
  border-radius: 12px;
  padding: 20px;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
}

.step-header {
  display: flex;
  align-items: center;
  margin-bottom: 16px;
}

.step-number {
  width: 32px;
  height: 32px;
  background: linear-gradient(135deg, #3cc51f 0%, #2e8b57 100%);
  color: white;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 18px;
  font-weight: bold;
  margin-right: 12px;
  flex-shrink: 0;
}

.step-title {
  font-size: 20px;
  font-weight: bold;
  color: #333;
}

.step-content {
  padding-left: 44px;
}

.step-text {
  font-size: 16px;
  color: #555;
  line-height: 1.8;
  display: block;
}

.step-text.mt {
  margin-top: 12px;
}

.step-detail {
  margin: 12px 0;
  padding-left: 8px;
}

.detail-item {
  font-size: 16px;
  color: #666;
  line-height: 2;
  display: block;
}

.step-tip {
  font-size: 15px;
  color: #3cc51f;
  line-height: 1.8;
  display: block;
  margin-top: 12px;
  padding: 12px;
  background-color: #f0f9eb;
  border-radius: 8px;
}

/* 小提示区域 */
.tips-section {
  margin-top: 10px;
}

.tip-card {
  background-color: white;
  margin: 0 20px;
  border-radius: 12px;
  padding: 20px;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
}

.tip-item {
  font-size: 16px;
  color: #666;
  line-height: 2.2;
  display: block;
}

/* 底部 */
.footer {
  margin-top: 40px;
  padding: 20px;
  display: flex;
  flex-direction: column;
  align-items: center;
}

.footer-text {
  font-size: 14px;
  color: #999;
  line-height: 1.8;
}
