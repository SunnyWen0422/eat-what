# 云服务器部署清单

## 📋 服务器信息

```yaml
服务器名称: instance1761731222975R290
实例ID: i-ye7xyk2kg0xjd1vl0js8
公网IP: 115.190.206.149
私网IP: 192.168.74.177
域名: chishenme.icu
SSL证书ID: 21211380
```

---

## ✅ 部署步骤清单

### 阶段一：服务器准备

- [ ] **连接到服务器**
  ```bash
  ssh root@115.190.206.149
  ```

- [ ] **上传并执行环境配置脚本**
  ```bash
  # 本地执行
  scp backend/server-setup.sh root@115.190.206.149:/root/
  
  # 服务器执行
  chmod +x /root/server-setup.sh
  /root/server-setup.sh
  ```

- [ ] **验证软件安装**
  ```bash
  java -version          # 应显示 1.8.x
  mysql --version        # 应显示 8.0.x
  nginx -v              # 应显示版本号
  ```

---

### 阶段二：数据库配置

- [ ] **上传SQL脚本**
  ```bash
  scp backend/init_database.sql root@115.190.206.149:/root/
  ```

- [ ] **执行SQL脚本**
  ```bash
  # 在服务器上执行
  mysql -u eatwhat -pSunny418@Cloud food < /root/init_database.sql
  ```

- [ ] **验证数据库**
  ```bash
  mysql -u eatwhat -pSunny418@Cloud food
  
  # 在MySQL中执行
  SELECT COUNT(*) FROM food;
  SELECT TYPE, COUNT(*) FROM food GROUP BY TYPE;
  SHOW TABLES;
  EXIT;
  ```
  
  ✅ 预期结果：
  - food表至少有20条数据
  - 包含meat、veg、soup三种类型
  - 存在users和custom_recipes表

---

### 阶段三：后端部署

- [ ] **打包项目（本地）**
  ```bash
  cd backend
  mvn clean package -DskipTests
  ```
  
  ✅ 生成文件：`target/eatwhat-backend-1.0.0.jar`

- [ ] **上传到服务器**
  ```bash
  scp target/eatwhat-backend-1.0.0.jar root@115.190.206.149:/root/
  scp src/main/resources/application-prod.yml root@115.190.206.149:/root/
  ```

- [ ] **启动后端服务**
  ```bash
  # 在服务器上执行
  sudo systemctl start eatwhat
  sudo systemctl status eatwhat
  ```

- [ ] **验证后端运行**
  ```bash
  # 在服务器上执行
  curl http://localhost:8080/api/dishes
  
  # 查看日志
  sudo journalctl -u eatwhat -f
  ```
  
  ✅ 预期结果：返回JSON格式的菜品数据

---

### 阶段四：域名配置

- [ ] **配置DNS解析**
  
  在域名服务商（阿里云/腾讯云等）添加DNS记录：
  
  | 类型 | 主机记录 | 记录值 | TTL |
  |------|----------|--------|-----|
  | A | @ | 115.190.206.149 | 600 |
  | A | www | 115.190.206.149 | 600 |

- [ ] **验证DNS解析**
  ```bash
  # 在本地执行（等待5-10分钟）
  nslookup chishenme.icu
  ping chishenme.icu
  ```
  
  ✅ 预期结果：返回IP `115.190.206.149`

---

### 阶段五：SSL证书配置

- [ ] **下载SSL证书**
  
  从云服务商下载：
  - 证书文件：`chishenme.icu.pem` 或 `.crt`
  - 私钥文件：`chishenme.icu.key`

- [ ] **上传证书到服务器**
  ```bash
  # 本地执行
  scp chishenme.icu.pem root@115.190.206.149:/etc/nginx/ssl/
  scp chishenme.icu.key root@115.190.206.149:/etc/nginx/ssl/
  ```

- [ ] **设置证书权限**
  ```bash
  # 服务器执行
  sudo chmod 600 /etc/nginx/ssl/chishenme.icu.key
  sudo chmod 644 /etc/nginx/ssl/chishenme.icu.pem
  ```

- [ ] **验证证书文件**
  ```bash
  ls -la /etc/nginx/ssl/
  ```
  
  ✅ 应看到两个文件

---

### 阶段六：Nginx配置

- [ ] **上传Nginx配置**
  ```bash
  # 本地执行
  scp backend/nginx-chishenme.conf root@115.190.206.149:/etc/nginx/conf.d/
  ```

- [ ] **修改证书路径（如果文件名不同）**
  ```bash
  # 服务器执行
  sudo nano /etc/nginx/conf.d/nginx-chishenme.conf
  
  # 确认这两行的文件名正确：
  ssl_certificate /etc/nginx/ssl/chishenme.icu.pem;
  ssl_certificate_key /etc/nginx/ssl/chishenme.icu.key;
  ```

- [ ] **测试Nginx配置**
  ```bash
  sudo nginx -t
  ```
  
  ✅ 应显示：syntax is ok, test is successful

- [ ] **重启Nginx**
  ```bash
  sudo systemctl restart nginx
  sudo systemctl status nginx
  ```

- [ ] **验证Nginx运行**
  ```bash
  # 测试HTTP（应重定向到HTTPS）
  curl -I http://chishenme.icu
  
  # 测试HTTPS
  curl https://chishenme.icu/api/dishes
  ```
  
  ✅ 预期结果：返回菜品JSON数据

---

### 阶段七：小程序配置

- [ ] **更新小程序配置**
  
  修改 `utils/config.js`：
  ```javascript
  const USE_BACKEND_API = true  // ✅ 已修改
  const PROD_API_BASE_URL = 'https://chishenme.icu/api'
  ```

- [ ] **测试浏览器访问**
  
  在浏览器打开：
  ```
  https://chishenme.icu/api/dishes
  ```
  
  ✅ 应显示JSON数据

---

### 阶段八：微信公众平台配置

- [ ] **登录微信公众平台**
  
  访问：https://mp.weixin.qq.com/

- [ ] **配置服务器域名**
  
  路径：开发 → 开发管理 → 开发设置 → 服务器域名
  
  **request合法域名：**
  ```
  https://chishenme.icu
  ```
  
  ⚠️ 注意：
  - 必须以 `https://` 开头
  - 不要加端口号
  - 不要加 `/api` 路径

- [ ] **保存配置**
  
  点击保存，等待生效（通常立即生效）

---

### 阶段九：测试验证

- [ ] **测试域名和SSL**
  ```bash
  # 在本地执行
  curl -I https://chishenme.icu
  curl https://chishenme.icu/api/dishes | head -n 20
  ```
  
  ✅ 应返回200状态码和数据

- [ ] **测试小程序**
  
  1. 打开微信开发者工具
  2. **取消勾选**"不校验合法域名"
  3. 编译运行小程序
  4. 点击"定制菜谱"
  
  ✅ 应能看到菜品列表

- [ ] **检查网络请求**
  
  在微信开发者工具的 Network 面板：
  - 请求地址：`https://chishenme.icu/api/dishes`
  - 状态码：200
  - 有返回数据

- [ ] **测试完整功能**
  - [ ] 查看菜品列表
  - [ ] 搜索菜品
  - [ ] 添加自定义菜品
  - [ ] 生成推荐方案
  - [ ] 保存菜谱

---

## 🔒 安全配置清单

- [ ] **配置防火墙**
  ```bash
  # CentOS
  sudo firewall-cmd --list-all
  
  # Ubuntu
  sudo ufw status
  ```

- [ ] **MySQL安全配置**
  ```bash
  # 确认MySQL只监听本地
  sudo netstat -tuln | grep 3306
  ```
  
  ✅ 应显示 `127.0.0.1:3306`

- [ ] **修改默认密码**
  
  将 `Sunny418@Cloud` 改为更强的密码

- [ ] **定期备份数据库**
  ```bash
  # 创建备份脚本
  mysqldump -u eatwhat -pSunny418@Cloud food > /root/backup_$(date +%Y%m%d).sql
  ```

---

## 📊 监控清单

- [ ] **设置日志轮转**
  ```bash
  # 检查日志大小
  du -h /var/log/nginx/
  du -h /var/log/eatwhat/
  ```

- [ ] **监控磁盘空间**
  ```bash
  df -h
  ```

- [ ] **监控内存使用**
  ```bash
  free -h
  ```

- [ ] **设置告警**（可选）
  - 磁盘空间不足
  - 服务宕机
  - CPU/内存过高

---

## 🔄 日常维护清单

### 每周

- [ ] 查看错误日志
  ```bash
  sudo tail -n 100 /var/log/nginx/chishenme_error.log
  sudo journalctl -u eatwhat --since "1 week ago" | grep ERROR
  ```

- [ ] 查看访问统计
  ```bash
  sudo awk '{print $1}' /var/log/nginx/chishenme_access.log | sort | uniq -c | sort -rn | head -n 10
  ```

### 每月

- [ ] 备份数据库
  ```bash
  mysqldump -u eatwhat -pSunny418@Cloud food > /root/backup_monthly_$(date +%Y%m%d).sql
  ```

- [ ] 更新系统
  ```bash
  sudo yum update -y  # CentOS
  sudo apt update && sudo apt upgrade -y  # Ubuntu
  ```

- [ ] 检查SSL证书有效期
  ```bash
  echo | openssl s_client -connect chishenme.icu:443 2>/dev/null | openssl x509 -noout -dates
  ```

---

## 🆘 故障排查清单

### 问题：域名无法访问

- [ ] DNS是否生效
  ```bash
  nslookup chishenme.icu
  ```

- [ ] 防火墙是否开放
  ```bash
  sudo firewall-cmd --list-all  # CentOS
  sudo ufw status  # Ubuntu
  ```

- [ ] Nginx是否运行
  ```bash
  sudo systemctl status nginx
  ```

### 问题：SSL证书错误

- [ ] 证书文件是否存在
  ```bash
  ls -la /etc/nginx/ssl/
  ```

- [ ] 证书权限是否正确
  ```bash
  sudo chmod 600 /etc/nginx/ssl/*.key
  sudo chmod 644 /etc/nginx/ssl/*.pem
  ```

- [ ] 证书是否过期
  ```bash
  openssl x509 -in /etc/nginx/ssl/chishenme.icu.pem -noout -dates
  ```

### 问题：API返回错误

- [ ] 后端服务是否运行
  ```bash
  sudo systemctl status eatwhat
  ```

- [ ] 查看后端日志
  ```bash
  sudo journalctl -u eatwhat -n 50
  ```

- [ ] 测试本地连接
  ```bash
  curl http://localhost:8080/api/dishes
  ```

### 问题：小程序无法调用

- [ ] 微信域名是否配置
  - 登录微信公众平台查看

- [ ] 域名是否使用HTTPS
  ```bash
  curl -I https://chishenme.icu
  ```

- [ ] 开发者工具是否取消域名校验
  - 取消勾选"不校验合法域名"

---

## 📞 联系方式

### 查看服务器状态

```bash
# 系统信息
uname -a
cat /etc/os-release

# 服务状态
sudo systemctl status eatwhat
sudo systemctl status nginx
sudo systemctl status mysqld

# 端口监听
sudo netstat -tuln | grep -E ':(80|443|8080|3306)'

# 进程信息
ps aux | grep java
ps aux | grep nginx
ps aux | grep mysql
```

### 常用命令

```bash
# 重启服务
sudo systemctl restart eatwhat
sudo systemctl restart nginx

# 查看日志
sudo journalctl -u eatwhat -f
sudo tail -f /var/log/nginx/chishenme_error.log

# 测试API
curl https://chishenme.icu/api/dishes
```

---

## ✅ 最终检查

全部完成后，确认以下所有项：

### 服务器层面
- [x] Java、MySQL、Nginx已安装
- [x] 防火墙已配置
- [x] 数据库已初始化
- [x] 后端服务已启动并设置开机自启

### 网络层面
- [x] DNS解析正确
- [x] 80端口可访问（自动跳转443）
- [x] 443端口可访问（HTTPS）
- [x] SSL证书有效

### 应用层面
- [x] API接口返回正确数据
- [x] Nginx代理配置正确
- [x] 跨域配置正确

### 小程序层面
- [x] 配置文件已更新
- [x] 微信服务器域名已配置
- [x] 小程序可以正常调用API
- [x] 所有功能测试通过

---

## 🎉 部署完成

**恭喜！您已成功将小程序部署到生产环境！**

**访问地址：**
- API: https://chishenme.icu/api
- 测试接口: https://chishenme.icu/api/dishes

**后续工作：**
1. 添加更多菜品数据
2. 优化性能和用户体验
3. 添加数据分析功能
4. 定期备份和维护

---

**部署时间：** ___________  
**部署人员：** ___________  
**服务器管理员：** ___________

