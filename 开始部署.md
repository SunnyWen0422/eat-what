# 🚀 开始部署到云服务器

## 您的配置信息

✅ 已经准备好：
- **服务器IP**: 115.190.206.149
- **域名**: chishenme.icu  
- **SSL证书**: 已申请（证书ID: 21211380）
- **小程序配置**: 已开启API模式

---

## 快速开始 - 三种方式

### 🎯 方式一：自动化脚本（推荐）⭐

**最快速的部署方式，约15分钟完成！**

#### 步骤1：上传脚本

```bash
# 在本地项目目录执行
scp backend/server-setup.sh root@115.190.206.149:/root/
```

#### 步骤2：运行脚本

```bash
# 连接服务器
ssh root@115.190.206.149

# 执行脚本
chmod +x /root/server-setup.sh
/root/server-setup.sh
```

**脚本会自动完成：**
- ✅ 安装Java、MySQL、Nginx
- ✅ 配置防火墙
- ✅ 创建数据库和用户
- ✅ 创建systemd服务
- ✅ 设置目录权限

#### 步骤3：部署应用

```bash
# 本地打包并部署（在项目backend目录）
cd backend
chmod +x deploy.sh
./deploy.sh
```

**部署脚本会自动：**
- ✅ 打包项目
- ✅ 上传到服务器
- ✅ 重启服务
- ✅ 测试接口

---

### 📋 方式二：按清单操作（稳妥）

打开 **`云服务器部署清单.md`**，按照清单逐项完成。

**优点：**
- 每一步都有验证
- 适合新手
- 不容易出错

**预计时间：** 30-40分钟

---

### 📖 方式三：完整文档（详细）

查看 **`docs/CLOUD_DEPLOYMENT.md`**，这是最详细的部署指南。

**包含：**
- 完整的命令说明
- 配置文件详解
- 故障排查方法
- 安全配置建议

---

## 🔑 关键步骤提醒

### 1. SSL证书配置（重要！）

您需要从云服务商下载SSL证书文件：

**阿里云下载位置：**
- 证书管理 → 选择证书 → 下载 → Nginx格式

**会得到两个文件：**
- `chishenme.icu.pem`（证书文件）
- `chishenme.icu.key`（私钥文件）

**上传到服务器：**
```bash
scp chishenme.icu.pem root@115.190.206.149:/etc/nginx/ssl/
scp chishenme.icu.key root@115.190.206.149:/etc/nginx/ssl/
```

### 2. DNS解析配置

在您的域名管理控制台添加：

| 记录类型 | 主机记录 | 记录值 |
|---------|---------|--------|
| A | @ | 115.190.206.149 |
| A | www | 115.190.206.149 |

**等待时间：** 5-10分钟生效

**验证：**
```bash
nslookup chishenme.icu
```

### 3. 微信小程序域名配置

**登录：** https://mp.weixin.qq.com/

**路径：** 开发 → 开发管理 → 开发设置 → 服务器域名

**添加：**
```
request合法域名: https://chishenme.icu
```

---

## ⚡ 快速命令参考

### 连接服务器
```bash
ssh root@115.190.206.149
```

### 检查服务状态
```bash
# 后端服务
sudo systemctl status eatwhat

# Nginx
sudo systemctl status nginx

# MySQL
sudo systemctl status mysqld  # 或 mysql
```

### 查看日志
```bash
# 后端日志
sudo journalctl -u eatwhat -f

# Nginx错误日志
sudo tail -f /var/log/nginx/chishenme_error.log

# Nginx访问日志
sudo tail -f /var/log/nginx/chishenme_access.log
```

### 重启服务
```bash
sudo systemctl restart eatwhat
sudo systemctl restart nginx
```

### 测试API
```bash
# 服务器本地测试
curl http://localhost:8080/api/dishes

# 域名测试
curl https://chishenme.icu/api/dishes
```

---

## 📝 部署流程概览

```
1. 服务器环境配置 (5分钟)
   ├── 连接服务器
   ├── 安装软件（Java/MySQL/Nginx）
   └── 配置防火墙

2. 数据库部署 (3分钟)
   ├── 创建数据库
   ├── 导入SQL脚本
   └── 验证数据

3. 后端部署 (5分钟)
   ├── 打包项目
   ├── 上传jar包
   ├── 启动服务
   └── 配置开机自启

4. Nginx配置 (5分钟)
   ├── 上传SSL证书
   ├── 配置Nginx
   └── 重启Nginx

5. 域名配置 (2分钟)
   ├── 添加DNS记录
   └── 验证解析

6. 小程序配置 (2分钟)
   ├── 更新配置文件（已完成✅）
   └── 配置微信域名

7. 测试验证 (3分钟)
   ├── 测试API
   ├── 测试小程序
   └── 功能测试

总计：约25分钟
```

---

## ✅ 成功标志

部署成功后，应满足：

### 服务器端
- [ ] 浏览器访问 `https://chishenme.icu/api/dishes` 返回JSON数据
- [ ] SSL证书显示有效（浏览器地址栏有锁图标）
- [ ] 后端服务自动启动（重启服务器后仍运行）

### 小程序端
- [ ] 取消勾选"不校验域名"后，小程序仍能正常使用
- [ ] Network面板显示请求地址为 `https://chishenme.icu/api/...`
- [ ] 所有功能正常（查看、搜索、添加、推荐）

---

## 🆘 遇到问题？

### 常见问题快速解决

**❌ 无法连接服务器**
```bash
# 检查IP是否正确
ping 115.190.206.149

# 检查SSH端口
telnet 115.190.206.149 22
```

**❌ 域名无法访问**
```bash
# 检查DNS
nslookup chishenme.icu

# 检查防火墙
sudo firewall-cmd --list-all  # CentOS
sudo ufw status  # Ubuntu
```

**❌ SSL证书错误**
- 确认证书文件已上传
- 检查文件名是否匹配Nginx配置
- 验证证书未过期

**❌ API返回404**
- 确认后端服务已启动
- 检查Nginx配置中的proxy_pass
- 查看后端日志

### 获取帮助

1. **查看详细文档**
   - `docs/CLOUD_DEPLOYMENT.md` - 完整部署指南
   - `云服务器部署清单.md` - 操作清单

2. **查看日志**
   - 后端：`sudo journalctl -u eatwhat -n 50`
   - Nginx：`sudo tail -n 50 /var/log/nginx/chishenme_error.log`

3. **运行诊断命令**
   ```bash
   # 检查端口
   sudo netstat -tuln | grep -E ':(80|443|8080)'
   
   # 检查进程
   ps aux | grep java
   ps aux | grep nginx
   ```

---

## 📁 相关文件

### 配置文件
- `backend/application-prod.yml` - 生产环境配置
- `backend/nginx-chishenme.conf` - Nginx配置
- `utils/config.js` - 小程序配置（已更新✅）

### 脚本文件
- `backend/server-setup.sh` - 服务器环境配置脚本
- `backend/deploy.sh` - 自动部署脚本

### 文档
- `docs/CLOUD_DEPLOYMENT.md` - 完整部署指南⭐
- `云服务器部署清单.md` - 操作清单⭐
- `开始部署.md` - 本文件

---

## 🎯 推荐部署路径

**首次部署（新服务器）：**
```
1. 阅读本文（5分钟）
2. 使用自动化脚本配置环境（5分钟）
3. 手动配置SSL和域名（10分钟）
4. 测试验证（5分钟）
```

**后续更新：**
```
1. 修改代码
2. 运行 ./backend/deploy.sh
3. 验证功能
```

---

## 🚀 现在开始！

选择您喜欢的方式：

### ⚡ 方式一：自动化（推荐）
```bash
# 步骤1：上传配置脚本
scp backend/server-setup.sh root@115.190.206.149:/root/

# 步骤2：执行配置
ssh root@115.190.206.149
chmod +x /root/server-setup.sh
/root/server-setup.sh
```

### 📋 方式二：按清单
打开 `云服务器部署清单.md`，跟着清单一步步做。

### 📖 方式三：看文档
打开 `docs/CLOUD_DEPLOYMENT.md`，查看完整说明。

---

**预祝部署顺利！** 🎉

有任何问题请查看详细文档或检查日志。部署完成后，您的小程序就可以正式上线了！

