# ⚡ 立即开始 - 云服务器部署

## 🎯 您的目标
将"吃什么"小程序从本地开发环境部署到云服务器，实现：
- ✅ 通过域名 `chishenme.icu` 访问
- ✅ 使用HTTPS安全连接
- ✅ 小程序正式上线

---

## 📦 您已拥有的资源

✅ **云服务器**
- IP: 115.190.206.149
- 已开通和配置

✅ **域名**
- chishenme.icu
- 已申请

✅ **SSL证书**
- 已申请测试版证书
- 证书ID: 21211380

✅ **项目代码**
- 后端服务已开发完成
- 小程序已配置API模式

---

## 🚀 三步完成部署

### 第一步：配置服务器环境（5分钟）

**在本地电脑执行：**

```bash
# 1. 上传配置脚本
cd "C:\Users\Administrator\WeChatProjects\吃什么"
scp backend/server-setup.sh root@115.190.206.149:/root/
```

**连接服务器执行：**

```bash
# 2. 连接服务器
ssh root@115.190.206.149

# 3. 运行配置脚本
chmod +x /root/server-setup.sh
/root/server-setup.sh
```

**脚本会自动安装：**
- Java 8
- MySQL 8
- Nginx
- 配置防火墙
- 创建数据库

⏰ **等待约5分钟完成**

---

### 第二步：下载并上传SSL证书（3分钟）

#### 2.1 下载SSL证书

**如果使用阿里云：**
1. 登录阿里云控制台
2. 进入"SSL证书"服务
3. 找到证书ID: 21211380
4. 点击"下载" → 选择"Nginx"格式
5. 下载后解压，得到两个文件：
   - `证书文件.pem`（改名为 `chishenme.icu.pem`）
   - `证书密钥.key`（改名为 `chishenme.icu.key`）

**如果使用腾讯云/其他：**
- 类似步骤，下载Nginx格式证书

#### 2.2 上传证书到服务器

```bash
# 在本地执行（证书文件所在目录）
scp chishenme.icu.pem root@115.190.206.149:/etc/nginx/ssl/
scp chishenme.icu.key root@115.190.206.149:/etc/nginx/ssl/
```

---

### 第三步：部署应用（5分钟）

#### 3.1 部署数据库

```bash
# 在本地执行
cd "C:\Users\Administrator\WeChatProjects\吃什么"
scp backend/init_database.sql root@115.190.206.149:/root/
```

```bash
# 在服务器执行
ssh root@115.190.206.149
mysql -u eatwhat -pSunny418@Cloud food < /root/init_database.sql
```

#### 3.2 部署后端

```bash
# 在本地项目backend目录执行
cd backend

# Windows PowerShell
mvn clean package -DskipTests

# 上传
scp target/eatwhat-backend-1.0.0.jar root@115.190.206.149:/root/
```

```bash
# 在服务器启动
ssh root@115.190.206.149
sudo systemctl start eatwhat
sudo systemctl status eatwhat
```

#### 3.3 配置Nginx

```bash
# 在本地执行
scp backend/nginx-chishenme.conf root@115.190.206.149:/etc/nginx/conf.d/
```

```bash
# 在服务器执行
sudo nginx -t
sudo systemctl restart nginx
```

---

## 🌐 第四步：配置DNS（2分钟）

### 在您的域名服务商添加DNS记录

**登录您购买 chishenme.icu 的服务商（如阿里云/腾讯云）**

添加两条A记录：

| 记录类型 | 主机记录 | 记录值 | TTL |
|---------|---------|--------|-----|
| A | @ | 115.190.206.149 | 600 |
| A | www | 115.190.206.149 | 600 |

**等待5-10分钟生效**

**验证DNS：**
```bash
# 在本地执行
nslookup chishenme.icu
```

应该返回：`115.190.206.149`

---

## 📱 第五步：配置微信小程序（2分钟）

### 5.1 配置服务器域名

1. 访问：https://mp.weixin.qq.com/
2. 登录小程序管理后台
3. 进入：**开发 → 开发管理 → 开发设置**
4. 找到：**服务器域名** → 点击**修改**
5. 添加request合法域名：
   ```
   https://chishenme.icu
   ```
6. 点击保存

### 5.2 测试小程序

1. 打开微信开发者工具
2. **取消勾选**"不校验合法域名、web-view（业务域名）、TLS版本"
3. 点击"编译"
4. 测试功能

---

## ✅ 验证部署成功

### 1. 测试API（浏览器）

打开浏览器，访问：
```
https://chishenme.icu/api/dishes
```

✅ **应显示：** JSON格式的菜品数据  
✅ **地址栏：** 有锁图标（HTTPS有效）

### 2. 测试小程序

在微信开发者工具中：
- [ ] 打开"定制菜谱"，能看到菜品列表
- [ ] 搜索功能正常
- [ ] 添加自定义菜品能保存
- [ ] 生成推荐方案正常

### 3. 检查网络请求

在开发者工具的Network面板：
- [ ] 请求地址：`https://chishenme.icu/api/dishes`
- [ ] 状态码：200
- [ ] 有数据返回

---

## 📊 完整命令速查

### 一键部署脚本（推荐）

如果您熟悉命令行，可以使用自动部署脚本：

```bash
# 在本地backend目录
cd backend
chmod +x deploy.sh
./deploy.sh
```

### 常用运维命令

```bash
# 连接服务器
ssh root@115.190.206.149

# 查看服务状态
sudo systemctl status eatwhat
sudo systemctl status nginx

# 查看日志
sudo journalctl -u eatwhat -f
sudo tail -f /var/log/nginx/chishenme_error.log

# 重启服务
sudo systemctl restart eatwhat
sudo systemctl restart nginx

# 测试API
curl https://chishenme.icu/api/dishes
```

---

## 🆘 遇到问题？

### 快速诊断

```bash
# 在服务器执行
# 检查服务是否运行
sudo systemctl status eatwhat
sudo systemctl status nginx

# 检查端口是否监听
sudo netstat -tuln | grep -E ':(80|443|8080|3306)'

# 测试本地API
curl http://localhost:8080/api/dishes

# 检查防火墙
sudo firewall-cmd --list-all  # CentOS
sudo ufw status  # Ubuntu
```

### 查看详细文档

- **完整部署指南：** `docs/CLOUD_DEPLOYMENT.md`
- **部署清单：** `云服务器部署清单.md`
- **快速开始：** `开始部署.md`

---

## 📞 关键文件位置

### 本地（您的电脑）
```
吃什么/
├── backend/
│   ├── server-setup.sh       ← 服务器配置脚本
│   ├── deploy.sh             ← 自动部署脚本
│   ├── init_database.sql     ← 数据库脚本
│   ├── nginx-chishenme.conf  ← Nginx配置
│   └── application-prod.yml  ← 生产配置
├── utils/
│   └── config.js             ← 小程序配置（已更新✅）
└── docs/
    └── CLOUD_DEPLOYMENT.md   ← 详细文档
```

### 服务器（115.190.206.149）
```
/root/
├── eatwhat-backend-1.0.0.jar  ← 后端jar包
├── init_database.sql          ← SQL脚本
└── server-setup.sh            ← 配置脚本

/etc/nginx/
├── conf.d/
│   └── nginx-chishenme.conf   ← Nginx配置
└── ssl/
    ├── chishenme.icu.pem      ← SSL证书
    └── chishenme.icu.key      ← SSL密钥
```

---

## 🎉 预期结果

部署完成后，您将拥有：

```
用户手机
    ↓
微信小程序
    ↓ HTTPS
chishenme.icu (115.190.206.149)
    ├── Nginx:443 (HTTPS + SSL)
    │   └── 反向代理 ↓
    ├── Spring Boot:8080 (后端API)
    │   └── 连接 ↓
    └── MySQL:3306 (数据库)
```

**访问地址：**
- API: https://chishenme.icu/api
- 测试: https://chishenme.icu/api/dishes

---

## ⏱️ 时间安排

- **第一步** - 服务器配置：5分钟
- **第二步** - SSL证书：3分钟
- **第三步** - 应用部署：5分钟
- **第四步** - DNS配置：2分钟
- **第五步** - 小程序配置：2分钟
- **验证测试**：3分钟

**总计：约20分钟**

---

## 🚀 现在就开始！

```bash
# 第一条命令 - 上传配置脚本
cd "C:\Users\Administrator\WeChatProjects\吃什么"
scp backend/server-setup.sh root@115.190.206.149:/root/
```

**然后按照上面的步骤继续...**

---

## 📝 部署后的下一步

1. **备份数据**
   ```bash
   mysqldump -u eatwhat -pSunny418@Cloud food > backup.sql
   ```

2. **监控服务**
   - 定期查看日志
   - 监控服务器资源

3. **优化性能**
   - 添加更多菜品数据
   - 优化数据库查询
   - 配置CDN加速

4. **提交审核**
   - 小程序提交审核
   - 等待微信审核通过
   - 正式发布！

---

**祝您部署顺利！** 🎊

遇到问题请查看详细文档或检查日志。

